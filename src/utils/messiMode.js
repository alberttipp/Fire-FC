// "Messi Mode" used to fire a canvas-confetti animation. Confetti has been
// removed (per product decision — too noisy, kept popping at the wrong times).
// Function kept as a no-op so existing call sites don't break; remove the
// imports and call sites incrementally.
export const triggerMessiMode = () => {
    // intentionally empty
};
